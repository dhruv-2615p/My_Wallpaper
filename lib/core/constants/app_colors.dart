import 'package:flutter/material.dart';

/// App-wide color constants for GyroWall.
class AppColors {
  AppColors._();

  // ── Dark Theme ──
  static const Color darkBackground = Color(0xFF0D0D0D);
  static const Color darkSurface = Color(0xFF1A1A1A);
  static const Color darkCard = Color(0xFF232323);
  static const Color darkText = Color(0xFFF5F5F5);

  // ── Light Theme ──
  static const Color lightBackground = Color(0xFFF2F2F2);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightCard = Color(0xFFFFFFFF);
  static const Color lightText = Color(0xFF1A1A1A);

  // ── Brand Colors ──
  static const Color primary = Color(0xFF6C63FF);
  static const Color secondary = Color(0xFF00BCD4);
  static const Color secondaryLight = Color(0xFF0097A7);

  // ── Preset Overlay Colors ──
  static const Color midnightBlue = Color(0xFF191970);
  static const Color roseGold = Color(0xFFB76E79);
  static const Color emerald = Color(0xFF50C878);
  static const Color sunsetOrange = Color(0xFFFD5E53);
  static const Color frostedWhite = Color(0xFFFFFFFF);

  // ── Water Default ──
  static const Color waterDefault = Color(0x4400BFFF);

  // ── Misc ──
  static const Color error = Color(0xFFCF6679);
  static const Color shimmerBase = Color(0xFF2A2A2A);
  static const Color shimmerHighlight = Color(0xFF3A3A3A);
}
